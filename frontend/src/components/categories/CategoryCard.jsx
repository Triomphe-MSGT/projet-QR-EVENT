import React from "react";

// const CarteCategorie = ({ categorie, onClick }) => {
//   const { id, nom, emoji } = categorie;

//   return (
//     <div
//       role="button"
//       tabIndex={0}
//       aria-label={`Explorer la catégorie ${nom}`}
//       onClick={() => onClick(id, nom)}
//       onKeyPress={(e) => e.key === "Enter" && onClick(id, nom)}
//     >
//       <div>{emoji}</div>
//       <h2>{nom}</h2>
//     </div>
//   );
// };

// export default CarteCategorie;
